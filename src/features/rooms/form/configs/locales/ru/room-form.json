{
	"fields": {
		"name": "Название",
		"description": "Описание"
	},
	"errors": {
		"name": {
			"empty": "$t(fields.name) не должно быть пустым",
			"min_length": "$t(fields.name) должен быть не короче {{ min_symbols_count }} символов",
			"max_length": "$t(fields.name) должен быть не длиннее {{ max_symbols_count }} символов"
		},
		"description": {
			"max_length": "$t(fields.description) должен быть не длиннее {{ max_symbols_count }} символов"
		}
	}
}
